[package]
name = "recording_master"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (os@miso.network)"]

# Intentionally media-agnostic: this extension operates on a bare `&mut UID` and
# never references miso, audio, or any concrete master type. Trust is carried
# by the ingester witness, not by a dependency. Only the Sui framework (implicit)
# is needed.
[dependencies]
