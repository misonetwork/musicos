[package]
name = "cover_art"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (os@miso.network)"]

[dependencies]
miso = { local = "../../core" }
ori = { git = "https://github.com/unconfirmedlabs/ori.git", subdir = "move", rev = "main" }
per_track = { local = "../../lib/per_track" }
