[package]
name = "genre"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (os@miso.network)"]

[dependencies]
miso = { local = "../../core" }
per_track = { local = "../../lib/per_track" }
